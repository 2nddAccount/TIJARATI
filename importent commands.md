# Important commands

to refresh the UI on the app: node .\bundler.js

to start the app on connected phone with usb debugging: cd mobile; npx expo run:android

to run a preview build: eas build --platform android --profile preview 